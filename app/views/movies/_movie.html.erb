<div class="movie" data-add-movie-target="new">
  <img src="<%= @movie.image_url %>" alt="">
  <div class="movie-body">
    <h2><%= @movie.title %></h2>
    <div class="movie-body-rating">
      <i class="fa-solid fa-star"></i>
      <%= @movie.rating %>
      <p>(<%=@movie.number_of_votes %> votes)</p>
    </div>
    <div class="movie-body-category">
      <%= render 'shared/categoryTag', categories: @movie.categories  %>
    </div>
    <p><%= @movie.length %></p>
    <h3>Description</h3>
    <%= @movie.description %>
    <h3>Réalisateurs</h3>
    <%= @movie.realisator %>
    <h3>Casting</h3>
    <%= @movie.actors %>
  </div>
</div>
